"""Job execution: planning now, live runs and scheduling later.

M2 provides planner.py, the dry run lifecycle. runner.py (live run supervision
and cancellation), scheduler.py (APScheduler) and archive.py (backup-dir paths)
arrive with M3, M4 and M6.
"""
